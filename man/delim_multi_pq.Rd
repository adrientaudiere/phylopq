% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delim_multi_pq.R
\name{delim_multi_pq}
\alias{delim_multi_pq}
\title{Compare several species delimitations of a phyloseq object}
\usage{
delim_multi_pq(
  physeq,
  methods = c("abgd", "asap"),
  slopes = c(0.5, 1, 1.5),
  model = 3,
  exe = NULL,
  tree = NULL,
  build_tree = FALSE,
  plot = TRUE,
  consensus = TRUE,
  n_match = NULL,
  delim_order = NULL,
  tbl_labs = NULL,
  col_vec = NULL,
  widths = c(0.5, 0.3),
  label_angle = 45,
  align = TRUE,
  align_method = c("decipher", "mafft"),
  mafft_exec = NULL,
  outfolder = NULL,
  keep_temporary_files = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
with a non-empty \code{refseq} slot.}

\item{methods}{Character vector of delimitation programs to run, any of
\code{"abgd"} and \code{"asap"}. Default to both.}

\item{slopes}{Numeric vector of relative gap widths, one ABGD run per value.
Default to \code{c(0.5, 1, 1.5)}. Ignored when \code{"abgd"} is not in \code{methods}.}

\item{model}{Integer, the evolutionary model used by the external programs.
0: Kimura-2P, 1: Jukes-Cantor, 2: Tamura-Nei, 3: simple p-distance
(default).}

\item{exe}{Named character vector of paths to the executables, e.g.
\code{c(abgd = "/usr/local/bin/abgd")}. Default to NULL, in which case each
program is looked up with \code{\link[=is_delim_installed]{is_delim_installed()}}.}

\item{tree}{The tree used to order and display the partitions. Either a
\code{phylo} object (\code{ape} package), a \code{treedata} object (\code{tidytree} package)
or NULL (default). When NULL the \code{phy_tree} slot of \code{physeq} is used, and
failing that a tree is built with \code{\link[MiscMetabar:build_phytree_pq]{MiscMetabar::build_phytree_pq()}} if
\code{build_tree = TRUE}.}

\item{build_tree}{Logical, if TRUE and no tree is available a maximum
likelihood tree is built from the alignment with
\code{\link[MiscMetabar:build_phytree_pq]{MiscMetabar::build_phytree_pq()}}. Default to FALSE, because building a
tree is by far the slowest step of this function.}

\item{plot}{Logical, if TRUE (default) a \code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}}
figure is returned in the \code{plot} element. Requires a tree.}

\item{consensus}{Logical, if TRUE (default) a consensus column computed by
\code{\link[delimtools:delim_consensus]{delimtools::delim_consensus()}} is added to the figure. Silently turned
off when fewer than two delimitations succeeded.}

\item{n_match}{Integer, the number of delimitations that must agree for the
consensus to call a species. Default to NULL, i.e. a simple majority
(at least two).}

\item{delim_order}{Character vector giving the order of the columns in the
figure. Default to NULL, i.e. the order the runs were performed in.}

\item{tbl_labs}{A two-column data.frame mapping tip labels (\code{label}) to the
labels shown on the figure (\code{labs}). Default to NULL, i.e. the tip labels
themselves.}

\item{col_vec}{Character vector of colours passed to
\code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}}. Default to NULL, i.e. a palette built with
\code{\link[delimtools:delim_brewer]{delimtools::delim_brewer()}}.}

\item{widths}{Relative widths of the tree panel and of the partition panel,
passed to \code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}}. Default to \code{c(0.5, 0.3)}, a
little wider than the \code{delimtools} default so that several runs fit.}

\item{label_angle}{Angle of the run names above the partition panel.
Default to 45 degrees, because run names such as \code{abgd_slope0.5} overlap
when written horizontally. Set to 0 for the \code{delimtools} look.}

\item{align}{Logical, if TRUE (default) reference sequences of unequal
length are aligned with \code{\link[=align_pq]{align_pq()}} before being submitted. The alignment
is computed once and reused by every run.}

\item{align_method}{Aligner used when \code{align = TRUE}, either \code{"decipher"}
(default, pure R) or \code{"mafft"} (external program, much faster on large
\code{refseq} slots). See \code{\link[=align_pq]{align_pq()}}.}

\item{mafft_exec}{Path to the MAFFT executable. Only used when
\code{align_method = "mafft"}. Default to NULL, i.e. the usual lookup of
\code{\link[=is_mafft_installed]{is_mafft_installed()}}.}

\item{outfolder}{Path to the folder the external programs write into. Each
run gets its own sub-folder, so that a run producing nothing cannot
inherit another run's partitions. Default to NULL, i.e. a temporary
location.}

\item{keep_temporary_files}{Logical, if TRUE the FASTA file and the output
folders are kept. Default to FALSE.}

\item{verbose}{Logical, if TRUE report each run and print the
\code{\link[delimtools:report_delim]{delimtools::report_delim()}} summary. Default to FALSE.}

\item{...}{Additional arguments passed on to
\code{\link[MiscMetabar:build_phytree_pq]{MiscMetabar::build_phytree_pq()}} when \code{build_tree = TRUE}.}
}
\value{
A list with five elements:
\describe{
\item{delim}{A named list of the partition tables, one per run.}
\item{joined}{A tibble with a \code{labels} column and one column per run,
whose partition names are harmonised across runs by
\code{\link[delimtools:delim_join]{delimtools::delim_join()}}, so that the same partition carries the same
name everywhere.}
\item{summary}{A data.frame giving the number of partitions each run
found.}
\item{plot}{The \code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}} figure, or NULL when
\code{plot = FALSE} or no tree was available.}
\item{tree}{The \code{phylo} object used for the figure, or NULL.}
}
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Run ABGD and ASAP several times on the reference sequences of a
\code{\link[phyloseq]{phyloseq-class}} object, join the resulting partitions
into a single table with \code{\link[delimtools:delim_join]{delimtools::delim_join()}}, and display them side
by side along a phylogenetic tree with \code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}}.

Where \code{\link[=delim_pq]{delim_pq()}} answers "which species does this taxon belong to?",
\code{delim_multi_pq()} answers "how stable is that answer?". A single barcode-gap
run is very sensitive to its parameters, so the usual practice is to scan a
range of relative gap widths and to compare the methods with each other
before trusting any one partition.

\code{slopes} drives the ABGD runs — one run per value, since the relative gap
width is the parameter ABGD is most sensitive to. ASAP has no equivalent
parameter (it scores and ranks partitions itself), so it contributes a
single run whatever \code{slopes} contains.
}
\details{
Run names are built from the method and its parameter, e.g.
\code{abgd_slope0.5}. Internally each run is submitted to
\code{\link[delimtools:delim_join]{delimtools::delim_join()}} under a digit-free alias, because that function
strips every digit from the delimitation names before matching partitions
across runs, which would collapse \code{abgd_slope0.5} and \code{abgd_slope1.5} into
a single column. The user-facing names are restored afterwards.

ABGD and ASAP are not available on Windows, and both need an aligned
FASTA — hence the \code{align = TRUE} default.
}
\examples{
\dontshow{if (phylopq::is_delim_installed("asap") && phylopq::is_delim_installed("abgd")) withAutoprint(\{ # examplesIf}
\donttest{
library(MiscMetabar)
data(data_fungi_mini)
df <- subset_taxa_pq(data_fungi_mini, taxa_sums(data_fungi_mini) > 9000)

# Scan three ABGD gap widths, add one ASAP run, and order the result along
# a taxonomy-based tree
res <- delim_multi_pq(
  df,
  slopes = c(0.5, 1, 1.5),
  tree = taxo2tree(df),
  verbose = TRUE
)
res$summary
res$joined
res$plot
}

\dontrun{
# Without a tree, only the tables are returned
res <- delim_multi_pq(df, plot = FALSE)

# Build a maximum likelihood tree on the fly (slow) and align with MAFFT
res <- delim_multi_pq(
  df,
  build_tree = TRUE,
  nb_bootstrap = 5,
  align_method = "mafft"
)

# ABGD only, on a finer grid of gap widths
res <- delim_multi_pq(df, methods = "abgd", slopes = seq(0.4, 1.6, by = 0.2))
}
\dontshow{\}) # examplesIf}
}
\references{
Puillandre N., Lambert A., Brouillet S., Achaz G. (2012) ABGD, Automatic
Barcode Gap Discovery for primary species delimitation. \emph{Molecular Ecology}
21(8):1864-1877. \doi{10.1111/j.1365-294X.2011.05239.x}

Puillandre N., Brouillet S., Achaz G. (2021) ASAP: assemble species by
automatic partitioning. \emph{Molecular Ecology Resources} 21:609-620.
\doi{10.1111/1755-0998.13281}
}
\seealso{
\code{\link[=delim_pq]{delim_pq()}}, \code{\link[=is_delim_installed]{is_delim_installed()}}, \code{\link[=align_pq]{align_pq()}},
\code{\link[MiscMetabar:build_phytree_pq]{MiscMetabar::build_phytree_pq()}}, \code{\link[delimtools:delim_autoplot]{delimtools::delim_autoplot()}}
}
\author{
Adrien Taudière
}
