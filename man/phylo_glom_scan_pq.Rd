% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylo_glom_pq.R
\name{phylo_glom_scan_pq}
\alias{phylo_glom_scan_pq}
\title{Scan several cophenetic thresholds before agglomerating}
\usage{
phylo_glom_scan_pq(physeq, h_values, hclust_method = "average")
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
with a non-empty \code{phy_tree} slot carrying branch lengths.}

\item{h_values}{(required) Numeric vector of cophenetic distance thresholds
to scan.}

\item{hclust_method}{Agglomeration method passed on to \code{\link[stats:hclust]{stats::hclust()}}.
Default to \code{"average"} (UPGMA), as in \code{\link[phyloseq:tip_glom]{phyloseq::tip_glom()}}.}
}
\value{
A data.frame with one row per value of \code{h_values} and the columns
\code{h} (the threshold), \code{n_taxa} (the resulting number of taxa) and
\code{prop_taxa} (that number divided by the initial number of taxa).
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Compute the number of taxa that \code{\link[=phylo_glom_pq]{phylo_glom_pq()}} would return
for a range of cophenetic thresholds, without building the agglomerated
objects. Useful to choose an informed value of \code{h} before running
\code{\link[=phylo_glom_pq]{phylo_glom_pq()}}.
}
\examples{
\donttest{
library(MiscMetabar)
data(data_fungi_mini)

pq <- add_tree_pq(
  data_fungi_mini,
  use_taxo_to_build_tree = TRUE,
  compute_brlen = TRUE
)
phylo_glom_scan_pq(pq, h_values = seq(0, 1, by = 0.1))
}

\dontrun{
scan <- phylo_glom_scan_pq(pq, h_values = seq(0, 1, by = 0.01))
plot(scan$h, scan$n_taxa, type = "l")
}
}
\seealso{
\code{\link[=phylo_glom_pq]{phylo_glom_pq()}}
}
\author{
Adrien Taudière
}
