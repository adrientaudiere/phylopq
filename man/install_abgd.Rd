% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_delim.R
\name{install_abgd}
\alias{install_abgd}
\title{Install the ABGD species-delimitation program}
\usage{
install_abgd(
  path = tools::R_user_dir("phylopq", "data"),
  src = NULL,
  force = FALSE
)
}
\arguments{
\item{path}{(default: \code{tools::R_user_dir("phylopq", "data")}) Directory in
which the \verb{bin/} sub-directory holding the executable is created.}

\item{src}{(default: NULL) Optional URL, local \code{.tar.gz} archive, or local
source directory to build from, bypassing the built-in sources. The
sources are copied before building, never modified in place.}

\item{force}{(default: FALSE) If TRUE, rebuild even when ABGD is already
installed.}
}
\value{
The path to the installed ABGD executable (invisibly).
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Download, compile and install ABGD (Automatic Barcode Gap Discovery,
Puillandre et al. 2012) into the phylopq user data directory, so that
\code{\link[=delim_pq]{delim_pq()}} and \code{\link[=is_delim_installed]{is_delim_installed()}} find it automatically.

ABGD is distributed as C sources, so a working toolchain (\code{make} and \code{gcc}
or \code{cc}) is required. On Debian or Ubuntu these come from \code{build-essential}.
}
\details{
The authors' server (\code{bioinfo.mnhn.fr}) has been unreachable since mid-2025.
The Internet Archive captured the last official ABGD tarball, which is
pristine upstream C, so it is tried first; the
\href{https://github.com/iTaxoTools/ABGDpy}{iTaxoTools mirror} and the canonical
URL follow.

On Windows, install the standalone executable from
\url{https://itaxotools.org/download.html} and point phylopq at it with
\code{options(phylopq.abgdpath = "C:/path/to/abgd.exe")}.
}
\examples{
\dontrun{
install_abgd()
is_delim_installed("abgd")

# From sources downloaded by hand
install_abgd(src = "~/Downloads/Abgd")
}
}
\references{
Puillandre N., Lambert A., Brouillet S., Achaz G. (2012) ABGD,
Automatic Barcode Gap Discovery for primary species delimitation.
\emph{Molecular Ecology} 21:1864-1877.
\doi{10.1111/j.1365-294X.2011.05239.x}
}
\seealso{
\code{\link[=install_asap]{install_asap()}}, \code{\link[=is_delim_installed]{is_delim_installed()}}, \code{\link[=delim_pq]{delim_pq()}}
}
\author{
Adrien Taudière
}
