% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delim_pq.R
\name{delim_pq}
\alias{delim_pq}
\title{Species delimitation of a phyloseq object with ABGD or ASAP}
\usage{
delim_pq(
  physeq,
  method = c("asap", "abgd"),
  exe = NULL,
  model = 3,
  slope = 1.5,
  webserver = NULL,
  outfolder = NULL,
  align = TRUE,
  merge_taxa = TRUE,
  tax_adjust = 1L,
  rank_propagation = TRUE,
  keep_temporary_files = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
with a non-empty \code{refseq} slot.}

\item{method}{One of \code{"asap"} (default) or \code{"abgd"}.}

\item{exe}{Path to the ABGD or ASAP executable. Ignored when \code{webserver} is
given. Default to NULL, in which case the executable is looked up on the
\code{PATH} with \code{\link[=is_delim_installed]{is_delim_installed()}}.}

\item{model}{Integer, the evolutionary model used by the external program.
0: Kimura-2P, 1: Jukes-Cantor, 2: Tamura-Nei, 3: simple p-distance
(default).}

\item{slope}{Numeric, relative gap width. Only used when
\code{method = "abgd"}. Default to 1.5.}

\item{webserver}{A result file obtained from the ABGD (\code{.txt}) or ASAP
(\code{.csv}) web server. When given, no executable is required. Default to
NULL.}

\item{outfolder}{Path to the folder where the external program writes its
output. Default to NULL, i.e. a temporary location.}

\item{align}{Logical, if TRUE (default) reference sequences of unequal
length are aligned with \code{\link[DECIPHER:AlignSeqs]{DECIPHER::AlignSeqs()}} before being submitted.
Both ABGD and ASAP require an aligned FASTA and silently return nothing
otherwise, so set \code{align = FALSE} only when \code{refseq} is already aligned.}

\item{merge_taxa}{Logical, if TRUE (default) taxa of the same partition are
merged and a phyloseq object is returned. If FALSE, the partition table is
returned untouched.}

\item{tax_adjust}{Handling of taxonomic disagreements within a partition.
See \code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}. Default to 1.}

\item{rank_propagation}{Logical, default TRUE, whether bad ranks are
propagated to lower ranks. See \code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}.}

\item{keep_temporary_files}{Logical, if TRUE the temporary FASTA file
submitted to the external program is kept. Default to FALSE.}

\item{verbose}{Logical, if TRUE report the number of taxa before and after
delimitation. Default to FALSE.}
}
\value{
A \code{\link[phyloseq]{phyloseq-class}} object whose taxa are the
delimited species, or a data.frame with columns \code{taxa} and \code{partition}
when \code{merge_taxa = FALSE}.
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Run a single-locus species-delimitation analysis on the
reference sequences of a \code{\link[phyloseq]{phyloseq-class}} object and
map the resulting partitions back onto its taxa.

Two barcode-gap methods are available through the \code{delimtools} package:
ABGD (Automatic Barcode Gap Discovery, Puillandre et al. 2012) and ASAP
(Assemble Species by Automatic Partitioning, Puillandre et al. 2021). Both
are external command-line programs: either give the path to the executable
with \code{exe}, or pass a result file downloaded from the corresponding web
server with \code{webserver}.

By default taxa belonging to the same partition are merged with
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}, so \code{delim_pq()} acts as a post-clustering
step comparable to \code{\link[MiscMetabar:postcluster_pq]{MiscMetabar::postcluster_pq()}}, but driven by a
barcode gap rather than by a fixed similarity threshold.
}
\details{
ABGD and ASAP are not available on Windows. Taxa that the external
program does not return a partition for are dropped by
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}} and reported with a warning.

Both programs need an aligned FASTA. Metabarcoding reference sequences are
almost never aligned, hence the \code{align = TRUE} default; alignment of a
large \code{refseq} slot can be slow.
}
\examples{
\dontshow{if (phylopq::is_delim_installed("asap")) withAutoprint(\{ # examplesIf}
library(MiscMetabar)
data(data_fungi_mini)
pq_asap <- delim_pq(data_fungi_mini, method = "asap", verbose = TRUE)
phyloseq::ntaxa(pq_asap)

\dontrun{
# Give an explicit path to the executable and inspect the partitions only
partitions <- delim_pq(
  data_fungi_mini,
  method = "abgd",
  exe = "/usr/local/bin/abgd",
  slope = 0.5,
  merge_taxa = FALSE
)
head(partitions)

# Reuse a result file downloaded from the ASAP web server
pq <- delim_pq(data_fungi_mini, method = "asap", webserver = "asap.csv")
}
\dontshow{\}) # examplesIf}
}
\references{
Puillandre N., Lambert A., Brouillet S., Achaz G. (2012) ABGD, Automatic
Barcode Gap Discovery for primary species delimitation. \emph{Molecular Ecology}
21(8):1864-1877. \doi{10.1111/j.1365-294X.2011.05239.x}

Puillandre N., Brouillet S., Achaz G. (2021) ASAP: assemble species by
automatic partitioning. \emph{Molecular Ecology Resources} 21:609-620.
\doi{10.1111/1755-0998.13281}
}
\seealso{
\code{\link[=is_delim_installed]{is_delim_installed()}}, \code{\link[MiscMetabar:postcluster_pq]{MiscMetabar::postcluster_pq()}},
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}
}
\author{
Adrien Taudière
}
