% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylo_glom_pq.R
\name{phylo_glom_pq}
\alias{phylo_glom_pq}
\title{Agglomerate taxa closer than a cophenetic distance threshold}
\usage{
phylo_glom_pq(
  physeq,
  h,
  hclust_method = "average",
  tax_adjust = 1L,
  rank_propagation = TRUE,
  return_map = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{physeq}{(required) A \code{\link[phyloseq]{phyloseq-class}} object
with a non-empty \code{phy_tree} slot carrying branch lengths.}

\item{h}{(required) Numeric, the cophenetic distance threshold at which the
tree-based dendrogram is cut. Taxa within \code{h} of each other are merged.}

\item{hclust_method}{Agglomeration method passed on to \code{\link[stats:hclust]{stats::hclust()}}.
Default to \code{"average"} (UPGMA), as in \code{\link[phyloseq:tip_glom]{phyloseq::tip_glom()}}.}

\item{tax_adjust}{Handling of taxonomic disagreements within a cluster. See
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}. Default to 1 (phyloseq-compatible).}

\item{rank_propagation}{Logical, default TRUE, whether bad ranks are
propagated to lower ranks. See \code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}.}

\item{return_map}{Logical, if TRUE return a data.frame mapping each taxon to
its cluster instead of the agglomerated phyloseq object. Default to FALSE.}

\item{verbose}{Logical, if TRUE report the number of taxa before and after
agglomeration. Default to FALSE.}
}
\value{
A \code{\link[phyloseq]{phyloseq-class}} object with agglomerated
taxa, or a data.frame with columns \code{taxa} and \code{cluster} when
\code{return_map = TRUE}.
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Merge taxa (ASV, OTU, ...) whose patristic (cophenetic)
distance on the \code{phy_tree} of a
\code{\link[phyloseq]{phyloseq-class}} object is below a threshold \code{h}.
Taxa are clustered with \code{\link[stats:hclust]{stats::hclust()}} on the cophenetic distance
matrix, cut at height \code{h} with \code{\link[stats:cutree]{stats::cutree()}}, and merged with
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}.

This is a faster and more transparent alternative to
\code{\link[phyloseq:tip_glom]{phyloseq::tip_glom()}}: the clustering method is explicit, the mapping
from taxa to clusters can be returned instead of the merged object, and
the number of merged taxa is reported.
}
\examples{
\donttest{
library(MiscMetabar)
data(data_fungi_mini)

pq <- add_tree_pq(
  data_fungi_mini,
  use_taxo_to_build_tree = TRUE,
  compute_brlen = TRUE
)
phyloseq::ntaxa(pq)

pq_glom <- phylo_glom_pq(pq, h = 0.2, verbose = TRUE)
phyloseq::ntaxa(pq_glom)

# Inspect the taxa-to-cluster mapping without merging
map <- phylo_glom_pq(pq, h = 0.2, return_map = TRUE)
head(map)
}

\dontrun{
# Keep the taxonomy strictly conservative across merged taxa
pq_glom <- phylo_glom_pq(pq, h = 0.2, tax_adjust = 2)
}
}
\seealso{
\code{\link[=phylo_glom_scan_pq]{phylo_glom_scan_pq()}}, \code{\link[=add_tree_pq]{add_tree_pq()}},
\code{\link[MiscMetabar:merge_taxa_vec]{MiscMetabar::merge_taxa_vec()}}, \code{\link[phyloseq:tip_glom]{phyloseq::tip_glom()}}
}
\author{
Adrien Taudière
}
