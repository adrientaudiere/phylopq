% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_delim.R
\name{install_asap}
\alias{install_asap}
\title{Install the ASAP species-delimitation program}
\usage{
install_asap(
  path = tools::R_user_dir("phylopq", "data"),
  src = NULL,
  force = FALSE
)
}
\arguments{
\item{path}{(default: \code{tools::R_user_dir("phylopq", "data")}) Directory in
which the \verb{bin/} sub-directory holding the executable is created.}

\item{src}{(default: NULL) Optional URL, local \code{.tar.gz} archive, or local
source directory to build from, bypassing the built-in sources. The
sources are copied before building, never modified in place.}

\item{force}{(default: FALSE) If TRUE, rebuild even when ASAP is already
installed.}
}
\value{
The path to the installed ASAP executable (invisibly).
}
\description{
\if{html}{\out{<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">}} \if{html}{\out{<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental">}}\if{html}{\out{</a>}}

Download, compile and install ASAP (Assemble Species by Automatic
Partitioning, Puillandre et al. 2021) into the phylopq user data directory,
so that \code{\link[=delim_pq]{delim_pq()}} and \code{\link[=is_delim_installed]{is_delim_installed()}} find it automatically.

ASAP is distributed as C sources, so a working toolchain (\code{make} and \code{gcc}
or \code{cc}) is required. On Debian or Ubuntu these come from \code{build-essential}.
}
\details{
The authors' server (\code{bioinfo.mnhn.fr}) has been unreachable since mid-2025
and no ASAP tarball was ever captured by the Internet Archive, so the
sources come from the \href{https://github.com/iTaxoTools/ASAPy}{iTaxoTools mirror}.
That copy carries a \code{wrapio.h} header redirecting stdio to Python, which
breaks the command-line build; phylopq neutralises it in a throwaway copy of
the sources before running \code{make}. The canonical URL is still tried last, so
this keeps working if the server returns.

On Windows, install the standalone executable from
\url{https://itaxotools.org/download.html} and point phylopq at it with
\code{options(phylopq.asappath = "C:/path/to/asap.exe")}.
}
\examples{
\dontrun{
install_asap()
is_delim_installed("asap")

# From sources downloaded by hand
install_asap(src = "~/Downloads/ASAPy-main")
}
}
\references{
Puillandre N., Brouillet S., Achaz G. (2021) ASAP: assemble
species by automatic partitioning. \emph{Molecular Ecology Resources}
21:609-620. \doi{10.1111/1755-0998.13281}
}
\seealso{
\code{\link[=install_abgd]{install_abgd()}}, \code{\link[=is_delim_installed]{is_delim_installed()}}, \code{\link[=delim_pq]{delim_pq()}}
}
\author{
Adrien Taudière
}
